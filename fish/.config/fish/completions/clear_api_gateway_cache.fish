# Completion script for clear_api_gateway_cache
complete -c clear_api_gateway_cache -a "prod" -d "Production environment"
complete -c clear_api_gateway_cache -a "dev" -d "Development environment"
complete -c clear_api_gateway_cache -a "staging" -d "Staging environment"
